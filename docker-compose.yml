services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: devops_platform
      POSTGRES_USER: devops
      POSTGRES_PASSWORD: devops
